/**
 * server.ts — MCP server entry point.
 *
 * Exposes three tools over the Model Context Protocol:
 *   - read_knowledge: query the knowledge base
 *   - write_knowledge: add a new knowledge entry
 *   - query_deps: query the dependency graph
 */

/**
 * Start the MCP server and register all tools.
 */
export async function startServer(): Promise<void> {
  throw new Error("Not implemented");
}
